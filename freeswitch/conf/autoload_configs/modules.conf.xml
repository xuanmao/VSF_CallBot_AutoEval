<configuration name="modules.conf" description="Modules">
  <modules>
    <!-- Loggers -->
    <load module="mod_console"/>
    <load module="mod_logfile"/>

    <!-- XML / config -->
    <load module="mod_xml_cdr"/>

    <!-- Event sockets / API -->
    <load module="mod_commands"/>
    <load module="mod_dptools"/>
    <load module="mod_dialplan_xml"/>
    <load module="mod_event_socket"/>

    <!-- Codecs (G.711 only — softphones all support it, FreeSWITCH
         transcodes to/from L16 for the bridge automatically) -->
    <load module="mod_sndfile"/>
    <load module="mod_native_file"/>
    <load module="mod_g711"/>

    <!-- SIP -->
    <load module="mod_sofia"/>

    <!-- Misc apps used by the dialplan -->
    <load module="mod_local_stream"/>
    <load module="mod_tone_stream"/>
    <load module="mod_loopback"/>

    <!-- The whole point of this project: per-call WebSocket audio bridge -->
    <load module="mod_audio_stream"/>
  </modules>
</configuration>
